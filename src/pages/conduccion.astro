---
import BaseHead from '../components/BaseHead.astro';
import Header from '../components/Header.astro';
import Footer from '../components/Footer.astro';
import { SITE_TITLE } from '../consts';
import { getCollection } from 'astro:content';

const posts = (await getCollection('blog')).sort(
    (a, b) => b.data.pubDate.valueOf() - a.data.pubDate.valueOf()
);

// Filtramos SOLO los de Conducción
const postsConduccion = posts.filter((post) => post.data.categoria === 'Conducción');
---

<!doctype html>
<html lang="es">
    <head>
        <BaseHead title={`Conducción | ${SITE_TITLE}`} description="Técnicas de conducción y seguridad para sobrevivir y dominar tu moto de 125cc." />
    </head>
    <body>
        <Header />
        <main>
            <h1>Técnicas de Conducción y Seguridad</h1>
            <section>
                <ul>
                    {
                        postsConduccion.map((post) => (
                            <li>
                                <a href={`/blog/${post.id}/`}>
                                    <h4 class="title">{post.data.title}</h4>
                                </a>
                            </li>
                        ))
                    }
                </ul>
            </section>
        </main>
        <Footer />
    </body>
</html>