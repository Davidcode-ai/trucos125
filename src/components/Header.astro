---
import HeaderLink from './HeaderLink.astro';
---

<header class="site-header">
  <div class="header-container">
    
    <a href="/" class="logo-link">
      <img src="/logo.png" alt="Trucos 125" class="logo-img" onerror="this.style.display='none'; this.nextElementSibling.style.display='block';" />
      <span class="fallback-logo" style="display:none; font-family:'Barlow Condensed', sans-serif; font-weight:900; font-size:2rem; color:#111; font-style:italic;">TRUCOS<span style="color:#e30613">125</span></span>
    </a>

    <button class="mobile-menu-btn" aria-label="Abrir menú">
      <svg viewBox="0 0 24 24" width="30" height="30" stroke="currentColor" stroke-width="2" fill="none" stroke-linecap="round" stroke-linejoin="round">
        <line x1="3" y1="12" x2="21" y2="12"></line>
        <line x1="3" y1="6" x2="21" y2="6"></line>
        <line x1="3" y1="18" x2="21" y2="18"></line>
      </svg>
    </button>

    <nav class="nav-links">
      <HeaderLink href="/">Inicio</HeaderLink>
      <HeaderLink href="/blog">Artículos</HeaderLink>
      <HeaderLink href="/sobre-mi">Sobre mí</HeaderLink>
      <HeaderLink href="/contacto">Contacto</HeaderLink>
    </nav>
    
  </div>
</header>

<style>
  /* --- EVITAR QUE LA WEB SALTE A LA DERECHA EN PÁGINAS CORTAS --- */
  :global(html) {
    overflow-y: scroll;
  }

  /* --- ESTILOS PRINCIPALES (ORDENADOR) --- */
  .site-header {
    background: #ffffff; 
    border-bottom: 3px solid #e30613; 
    box-shadow: 0 4px 10px rgba(0,0,0,0.05);
    position: sticky;
    top: 0;
    z-index: 999;
  }
  
  .header-container {
    max-width: 1280px;
    margin: 0 auto;
    padding: 0 20px;
    height: 80px;
    display: flex;
    align-items: center;
    justify-content: space-between;
  }

  .logo-link {
    display: flex;
    align-items: center;
    text-decoration: none;
  }

  .logo-img {
    width: 220px;
    height: auto;
    max-height: 75px; 
    object-fit: contain;
    transition: transform 0.2s ease;
  }
  
  .logo-link:hover .logo-img {
    transform: scale(1.05);
  }

  .nav-links {
    display: flex;
    gap: 30px;
    align-items: center;
  }

  /* --- TEXTO DEL MENÚ BLINDADO (No se parte) --- */
  .nav-links :global(a) {
    font-family: 'Barlow Condensed', sans-serif;
    color: #111;
    text-decoration: none;
    font-weight: 800;
    font-size: 18px !important; 
    text-transform: uppercase;
    transition: color 0.2s ease;
    white-space: nowrap; /* Evita que SOBRE MÍ se parta en dos líneas */
  }

  .nav-links :global(a:hover),
  .nav-links :global(a.active) {
    color: #e30613; 
  }

  .mobile-menu-btn {
    display: none; 
    background: none;
    border: none;
    color: #111;
    cursor: pointer;
    padding: 5px;
  }

  /* --- ESTILO ESPECIAL PARA EL BOTÓN DE CONTACTO --- */
  .nav-links :global(a[href="/contacto"]) {
    background-color: #e30613; 
    color: #ffffff !important; 
    padding: 10px 24px !important;
    border-radius: 8px; 
    margin-left: 15px; 
    border-bottom: none !important; 
    transition: all 0.3s ease;
  }

  .nav-links :global(a[href="/contacto"]:hover) {
    background-color: #b30009; 
    color: #ffffff !important;
    transform: translateY(-2px); 
    box-shadow: 0 4px 12px rgba(227, 6, 19, 0.3); 
  }

  /* --- RESPONSIVE (MÓVIL) --- */
  @media (max-width: 768px) {
    .header-container {
      height: 70px;
    }
    
    .logo-img {
      width: 160px; 
      height: auto;
    }
    
    .mobile-menu-btn {
      display: block; 
    }
    
    .nav-links {
      position: absolute;
      top: 73px; 
      left: 0;
      width: 100%;
      background: #ffffff;
      flex-direction: column;
      gap: 0;
      box-shadow: 0 10px 15px rgba(0,0,0,0.1);
      border-bottom: 3px solid #e30613;
      
      max-height: 0;
      overflow: hidden;
      transition: max-height 0.3s ease-out;
    }
    
    .nav-links.open {
      max-height: 350px; 
    }
    
    .nav-links :global(a) {
      padding: 15px 20px;
      width: 100%;
      border-bottom: 1px solid #f0f0f0;
      box-sizing: border-box;
      display: block;
      text-align: center;
      font-size: 16px !important; 
      margin: 0; /* Reseteamos margen en móvil */
    }
    
    .nav-links :global(a:last-child) {
      border-bottom: none;
    }

    /* Ajuste del botón Contacto en móvil */
    .nav-links :global(a[href="/contacto"]) {
      margin: 15px auto;
      width: calc(100% - 30px) !important;
    }
  }
</style>

<script>
  /* Script a prueba de fallos para menú móvil */
  document.addEventListener('astro:page-load', () => {
    const btn = document.querySelector('.mobile-menu-btn');
    const nav = document.querySelector('.nav-links');
    
    if (btn && nav) {
      btn.onclick = () => {
        nav.classList.toggle('open');
      };
    }
  });
</script>